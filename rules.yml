http:
  routers:
    router0:
      entryPoints:
        - web
      service: service-foo
      rule: host(`127.0.0.1`) && Pathprefix(`/schedule`)

  services:
    service-foo:
      loadBalancer:
        servers:
          - url: https://proseconsult.umontpellier.fr/jsp/custom/modules/plannings/direct_cal.jsp
        passHostHeader: true